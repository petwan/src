/**
 * Hooks 统一导出
 *
 * 注意：为了与 unplugin-auto-import 配合使用，
 * 建议直接在 vite.config.ts 中配置自动导入 src/hooks 目录，
 * 而不是通过此 index 文件手动导入。
 *
 * 例如，如果需要在某些特殊场景下手动导入：
 * import { useAppUpdate } from '@/hooks/useAppUpdate'
 */

